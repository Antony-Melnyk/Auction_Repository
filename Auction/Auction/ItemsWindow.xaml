<Window x:Class="Auction.ItemsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Auction"
        mc:Ignorable="d"
        Title="ItemsWindow" Height="300" Width="300">
    <Grid Background="AliceBlue">
        
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <ScrollViewer Grid.ColumnSpan="2" Grid.RowSpan="3" Background="LightBlue">
            <GroupBox Grid.ColumnSpan="2" Grid.RowSpan="3">
                <ListBox x:Name="itemslist" Foreground="Blue" FontSize="10" Background="AliceBlue"/>
            </GroupBox>
        </ScrollViewer>
        <Button Grid.Column="2" Margin="7" Background="CornflowerBlue" Content="Delete" FontSize="14" Foreground="Blue" Click="Delete_Click"/>
        <Button Grid.Column="2" Grid.Row="2" Background="LightCoral" Margin="15" FontSize="12" Foreground="Red" Content="Close" />
    </Grid>
</Window>
